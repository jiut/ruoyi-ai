# 设计师档案管理接口优化方案

## 📋 项目概述

基于用户档案自主管理需求，优化现有档案管理接口的权限控制，让设计师能够完整地管理自己的工作经历、教育背景、作品集和获奖记录，同时保持系统的安全性和权限控制。

## 🔧 接口设计架构（统一权限控制）

基于用户档案自主管理的原则，将档案管理接口统一设计，通过权限控制来区分操作范围：

### 接口设计原则

#### 1. 统一资源路径
- **路径规范**: 使用单数形式资源名，如 `/work`、`/award`
- **参数命名**: 使用具体的资源ID名称，如 `{workId}`、`{awardId}`
- **权限控制**: 通过后端权限检查控制用户只能操作自己的数据

#### 2. 智能权限分发
- **普通用户**: 只能查询和编辑自己的数据
- **管理员**: 可以查询和编辑所有数据
- **公开查询**: 特定查询接口支持公开访问

## 🔧 具体接口实现

### 1. 工作经历管理接口

**涉及控制器**: `WorkExperienceController.java`

```java
/**
 * 查询工作经历列表（权限控制返回范围）
 * - 普通用户：返回自己的工作经历
 * - 管理员：返回所有工作经历
 */
@GetMapping("/work-experience/list")
public R<List<WorkExperience>> getWorkExperienceList() {
    Long userId = LoginHelper.getUserId();
    
    // 检查用户权限
    if (permissionUtils.hasRole("admin")) {
        // 管理员可查看所有
        return R.ok(workExperienceService.selectWorkExperienceList(new WorkExperience()));
    } else {
        // 普通用户只能查看自己的
        Long designerId = getCurrentDesignerId();
        if (designerId == null) {
            return R.fail("用户未绑定设计师身份");
        }
        List<WorkExperience> experiences = workExperienceService.selectWorkExperienceByDesignerId(designerId);
        return R.ok(experiences);
    }
}

/**
 * 新增工作经历
 */
@Log(title = "工作经历", businessType = BusinessType.INSERT)
@PostMapping("/work-experience")
public R<Void> addWorkExperience(@Validated @RequestBody WorkExperience workExperience) {
    Long designerId = getCurrentDesignerId();
    if (designerId == null) {
        return R.fail("用户未绑定设计师身份");
    }
    
    // 强制设置为当前用户的设计师ID
    workExperience.setDesignerId(designerId);
    return toAjax(workExperienceService.insertWorkExperience(workExperience));
}

/**
 * 修改工作经历
 */
@Log(title = "工作经历", businessType = BusinessType.UPDATE)
@PutMapping("/work-experience/{experienceId}")
public R<Void> editWorkExperience(
    @PathVariable Long experienceId,
    @Validated @RequestBody WorkExperience workExperience) {
    
    // 检查记录是否存在且有权限操作
    WorkExperience existing = workExperienceService.selectWorkExperienceById(experienceId);
    if (existing == null) {
        return R.fail("工作经历不存在");
    }
    
    if (!hasPermissionToEdit(existing.getDesignerId())) {
        return R.fail("无权限编辑此工作经历");
    }
    
    // 确保不能修改设计师ID和记录ID
    workExperience.setExperienceId(experienceId);
    workExperience.setDesignerId(existing.getDesignerId());
    
    return toAjax(workExperienceService.updateWorkExperience(workExperience));
}

/**
 * 删除工作经历
 */
@Log(title = "工作经历", businessType = BusinessType.DELETE)
@DeleteMapping("/work-experience/{experienceId}")
public R<Void> removeWorkExperience(@PathVariable Long experienceId) {
    // 检查记录是否存在且有权限操作
    WorkExperience existing = workExperienceService.selectWorkExperienceById(experienceId);
    if (existing == null) {
        return R.fail("工作经历不存在");
    }
    
    if (!hasPermissionToEdit(existing.getDesignerId())) {
        return R.fail("无权限删除此工作经历");
    }
    
    return toAjax(workExperienceService.deleteWorkExperienceById(experienceId));
}

/**
 * 查询指定设计师的工作经历（公开访问）
 */
@GetMapping("/work-experience/designer/{designerId}")
public R<List<WorkExperience>> getDesignerWorkExperience(@PathVariable Long designerId) {
    if (designerId == null || designerId <= 0) {
        return R.fail("设计师ID无效");
    }
    
    List<WorkExperience> experiences = workExperienceService.selectWorkExperienceByDesignerId(designerId);
    return R.ok(experiences);
}
```

### 2. 教育背景管理接口

**涉及控制器**: `EducationController.java`

```java
/**
 * 查询教育背景列表（权限控制返回范围）
 */
@GetMapping("/education/list")
public R<List<Education>> getEducationList() {
    if (permissionUtils.hasRole("admin")) {
        return R.ok(educationService.selectEducationList(new Education()));
    } else {
        Long designerId = getCurrentDesignerId();
        if (designerId == null) {
            return R.fail("用户未绑定设计师身份");
        }
        List<Education> educations = educationService.selectEducationByDesignerId(designerId);
        return R.ok(educations);
    }
}

/**
 * 新增教育背景
 */
@Log(title = "教育背景", businessType = BusinessType.INSERT)
@PostMapping("/education")
public R<Void> addEducation(@Validated @RequestBody Education education) {
    Long designerId = getCurrentDesignerId();
    if (designerId == null) {
        return R.fail("用户未绑定设计师身份");
    }
    
    education.setDesignerId(designerId);
    return toAjax(educationService.insertEducation(education));
}

/**
 * 修改教育背景
 */
@Log(title = "教育背景", businessType = BusinessType.UPDATE)
@PutMapping("/education/{educationId}")
public R<Void> editEducation(
    @PathVariable Long educationId,
    @Validated @RequestBody Education education) {
    
    Education existing = educationService.selectEducationById(educationId);
    if (existing == null) {
        return R.fail("教育背景不存在");
    }
    
    if (!hasPermissionToEdit(existing.getDesignerId())) {
        return R.fail("无权限编辑此教育背景");
    }
    
    education.setEducationId(educationId);
    education.setDesignerId(existing.getDesignerId());
    
    return toAjax(educationService.updateEducation(education));
}

/**
 * 删除教育背景
 */
@Log(title = "教育背景", businessType = BusinessType.DELETE)
@DeleteMapping("/education/{educationId}")
public R<Void> removeEducation(@PathVariable Long educationId) {
    Education existing = educationService.selectEducationById(educationId);
    if (existing == null) {
        return R.fail("教育背景不存在");
    }
    
    if (!hasPermissionToEdit(existing.getDesignerId())) {
        return R.fail("无权限删除此教育背景");
    }
    
    return toAjax(educationService.deleteEducationById(educationId));
}

/**
 * 查询指定设计师的教育背景（公开访问）
 */
@GetMapping("/education/designer/{designerId}")
public R<List<Education>> getDesignerEducation(@PathVariable Long designerId) {
    if (designerId == null || designerId <= 0) {
        return R.fail("设计师ID无效");
    }
    
    List<Education> educations = educationService.selectEducationByDesignerId(designerId);
    return R.ok(educations);
}
```

### 3. 作品管理接口

**涉及控制器**: `WorkController.java`

```java
/**
 * 查询作品列表（权限控制返回范围）
 */
@GetMapping("/work/list")
public R<List<Work>> getWorkList() {
    if (permissionUtils.hasRole("admin")) {
        return R.ok(workService.selectWorkList(new Work()));
    } else {
        Long designerId = getCurrentDesignerId();
        if (designerId == null) {
            return R.fail("用户未绑定设计师身份");
        }
        List<Work> works = workService.selectWorkByDesignerId(designerId);
        return R.ok(works);
    }
}

/**
 * 新增作品
 */
@Log(title = "作品", businessType = BusinessType.INSERT)
@PostMapping("/work")
public R<Void> addWork(@Validated @RequestBody Work work) {
    Long designerId = getCurrentDesignerId();
    if (designerId == null) {
        return R.fail("用户未绑定设计师身份");
    }
    
    work.setDesignerId(designerId);
    return toAjax(workService.insertWork(work));
}

/**
 * 修改作品
 */
@Log(title = "作品", businessType = BusinessType.UPDATE)
@PutMapping("/work/{workId}")
public R<Void> editWork(
    @PathVariable Long workId,
    @Validated @RequestBody Work work) {
    
    Work existing = workService.selectWorkById(workId);
    if (existing == null) {
        return R.fail("作品不存在");
    }
    
    if (!hasPermissionToEdit(existing.getDesignerId())) {
        return R.fail("无权限编辑此作品");
    }
    
    work.setWorkId(workId);
    work.setDesignerId(existing.getDesignerId());
    
    return toAjax(workService.updateWork(work));
}

/**
 * 删除作品
 */
@Log(title = "作品", businessType = BusinessType.DELETE)
@DeleteMapping("/work/{workId}")
public R<Void> removeWork(@PathVariable Long workId) {
    Work existing = workService.selectWorkById(workId);
    if (existing == null) {
        return R.fail("作品不存在");
    }
    
    if (!hasPermissionToEdit(existing.getDesignerId())) {
        return R.fail("无权限删除此作品");
    }
    
    return toAjax(workService.deleteWorkById(workId));
}

/**
 * 查询指定设计师的作品（公开访问）
 */
@GetMapping("/work/designer/{designerId}")
public R<List<Work>> getDesignerWork(@PathVariable Long designerId) {
    if (designerId == null || designerId <= 0) {
        return R.fail("设计师ID无效");
    }
    
    List<Work> works = workService.selectWorkByDesignerId(designerId);
    return R.ok(works);
}
```

### 4. 获奖记录管理接口

**涉及控制器**: `AwardController.java`

```java
/**
 * 查询获奖记录列表（权限控制返回范围）
 */
@GetMapping("/award/list")
public R<List<Award>> getAwardList() {
    if (permissionUtils.hasRole("admin")) {
        return R.ok(awardService.selectAwardList(new Award()));
    } else {
        Long designerId = getCurrentDesignerId();
        if (designerId == null) {
            return R.fail("用户未绑定设计师身份");
        }
        List<Award> awards = awardService.selectAwardByDesignerId(designerId);
        return R.ok(awards);
    }
}

/**
 * 新增获奖记录
 */
@Log(title = "获奖记录", businessType = BusinessType.INSERT)
@PostMapping("/award")
public R<Void> addAward(@Validated @RequestBody Award award) {
    Long designerId = getCurrentDesignerId();
    if (designerId == null) {
        return R.fail("用户未绑定设计师身份");
    }
    
    award.setDesignerId(designerId);
    return toAjax(awardService.insertAward(award));
}

/**
 * 修改获奖记录
 */
@Log(title = "获奖记录", businessType = BusinessType.UPDATE)
@PutMapping("/award/{awardId}")
public R<Void> editAward(
    @PathVariable Long awardId,
    @Validated @RequestBody Award award) {
    
    Award existing = awardService.selectAwardById(awardId);
    if (existing == null) {
        return R.fail("获奖记录不存在");
    }
    
    if (!hasPermissionToEdit(existing.getDesignerId())) {
        return R.fail("无权限编辑此获奖记录");
    }
    
    award.setAwardId(awardId);
    award.setDesignerId(existing.getDesignerId());
    
    return toAjax(awardService.updateAward(award));
}

/**
 * 删除获奖记录
 */
@Log(title = "获奖记录", businessType = BusinessType.DELETE)
@DeleteMapping("/award/{awardId}")
public R<Void> removeAward(@PathVariable Long awardId) {
    Award existing = awardService.selectAwardById(awardId);
    if (existing == null) {
        return R.fail("获奖记录不存在");
    }
    
    if (!hasPermissionToEdit(existing.getDesignerId())) {
        return R.fail("无权限删除此获奖记录");
    }
    
    return toAjax(awardService.deleteAwardById(awardId));
}

/**
 * 查询指定设计师的获奖记录（公开访问）
 */
@GetMapping("/award/designer/{designerId}")
public R<List<Award>> getDesignerAward(@PathVariable Long designerId) {
    if (designerId == null || designerId <= 0) {
        return R.fail("设计师ID无效");
    }
    
    List<Award> awards = awardService.selectAwardByDesignerId(designerId);
    return R.ok(awards);
}
```

## 🛠️ 公共工具方法

### 通用权限检查方法

```java
/**
 * 获取当前用户的设计师ID
 */
private Long getCurrentDesignerId() {
    List<UserBinding> bindings = userBindingService.getBindingsByUserId(LoginHelper.getUserId());
    return extractDesignerId(bindings);
}

/**
 * 从绑定信息中提取设计师ID
 */
private Long extractDesignerId(List<UserBinding> bindings) {
    return bindings.stream()
        .filter(binding -> "designer".equals(binding.getEntityType()) && "1".equals(binding.getBindingStatus()))
        .map(UserBinding::getEntityId)
        .findFirst()
        .orElse(null);
}

/**
 * 检查用户是否有权限编辑指定设计师的数据
 */
private boolean hasPermissionToEdit(Long designerId) {
    // 管理员有所有权限
    if (permissionUtils.hasRole("admin")) {
        return true;
    }
    
    // 检查是否为当前用户的设计师ID
    Long currentDesignerId = getCurrentDesignerId();
    return currentDesignerId != null && currentDesignerId.equals(designerId);
}

/**
 * 基本参数验证 - 设计师ID
 */
private boolean isValidDesignerId(Long designerId) {
    return designerId != null && designerId > 0;
}
```

## ➕ 档案完整度计算接口

**文件**: `ruoyi-modules/ruoyi-designer/src/main/java/org/ruoyi/designer/controller/UserRegistrationController.java`

```java
/**
 * 获取用户档案完整度
 */
@Operation(
    summary = "获取档案完整度", 
    description = "计算当前用户各身份的档案完整度并提供改进建议"
)
@GetMapping("/profile/completeness")
public R<ProfileCompletenessVo> getProfileCompleteness() {
    Long userId = LoginHelper.getUserId();
    
    ProfileCompletenessVo completeness = new ProfileCompletenessVo();
    
    // 检查用户绑定的身份类型
    List<UserBinding> bindings = userBindingService.getBindingsByUserId(userId);
    
    for (UserBinding binding : bindings) {
        if (!"1".equals(binding.getBindingStatus())) continue; // 只处理有效绑定
        
        switch (binding.getEntityType()) {
            case "designer":
                DesignerCompletenessVo designerCompleteness = calculateDesignerCompleteness(binding.getEntityId());
                completeness.setDesigner(designerCompleteness);
                break;
            case "enterprise":
                EnterpriseCompletenessVo enterpriseCompleteness = calculateEnterpriseCompleteness(binding.getEntityId());
                completeness.setEnterprise(enterpriseCompleteness);
                break;
            case "school":
                SchoolCompletenessVo schoolCompleteness = calculateSchoolCompleteness(binding.getEntityId());
                completeness.setSchool(schoolCompleteness);
                break;
        }
    }
    
    return R.ok(completeness);
}

/**
 * 计算设计师档案完整度
 */
private DesignerCompletenessVo calculateDesignerCompleteness(Long designerId) {
    Designer designer = designerService.selectDesignerById(designerId);
    
    DesignerCompletenessVo completeness = new DesignerCompletenessVo();
    
    // 基础信息完整度 (权重40%)
    int basicScore = 0;
    if (StringUtils.isNotBlank(designer.getDesignerName())) basicScore += 20;
    if (StringUtils.isNotBlank(designer.getPhone())) basicScore += 20;
    if (StringUtils.isNotBlank(designer.getEmail())) basicScore += 20;
    if (StringUtils.isNotBlank(designer.getProfession())) basicScore += 20;
    if (StringUtils.isNotBlank(designer.getDescription())) basicScore += 20;
    completeness.setBasicInfo(basicScore);
    
    // 作品集完整度 (权重25%)
    List<Work> works = workService.selectWorkByDesignerId(designerId);
    int workScore = Math.min(works.size() * 10, 100);
    completeness.setPortfolio(workScore);
    
    // 工作经历完整度 (权重20%)
    List<WorkExperience> experiences = workExperienceService.selectWorkExperienceByDesignerId(designerId);
    int expScore = Math.min(experiences.size() * 25, 100);
    completeness.setExperience(expScore);
    
    // 教育背景完整度 (权重15%)
    List<Education> educations = educationService.selectEducationByDesignerId(designerId);
    int eduScore = Math.min(educations.size() * 50, 100);
    completeness.setEducation(eduScore);
    
    // 计算总体完整度
    int overall = (int) (basicScore * 0.4 + workScore * 0.25 + expScore * 0.2 + eduScore * 0.15);
    completeness.setOverall(overall);
    
    // 生成改进建议
    completeness.setSuggestions(generateImprovementSuggestions(completeness));
    
    return completeness;
}

/**
 * 生成改进建议
 */
private List<String> generateImprovementSuggestions(DesignerCompletenessVo completeness) {
    List<String> suggestions = new ArrayList<>();
    
    if (completeness.getBasicInfo() < 80) {
        suggestions.add("完善基础信息：建议补充个人简介、联系方式和职业技能");
    }
    
    if (completeness.getPortfolio() < 50) {
        suggestions.add("上传作品集：至少上传5个代表性作品以展示您的设计能力");
    }
    
    if (completeness.getExperience() < 50) {
        suggestions.add("添加工作经历：详细的工作经历有助于企业了解您的职业背景");
    }
    
    if (completeness.getEducation() < 50) {
        suggestions.add("补充教育背景：学历信息能够展示您的专业基础");
    }
    
    return suggestions;
}
```

## 📊 新增的VO类

### 1. 档案完整度响应类

**文件**: `ruoyi-modules-api/ruoyi-designer-api/src/main/java/org/ruoyi/designer/domain/vo/ProfileCompletenessVo.java`

```java
package org.ruoyi.designer.domain.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;

/**
 * 用户档案完整度信息
 */
@Data
@Schema(description = "用户档案完整度信息")
public class ProfileCompletenessVo {
    
    @Schema(description = "设计师档案完整度")
    private DesignerCompletenessVo designer;
    
    @Schema(description = "企业档案完整度")
    private EnterpriseCompletenessVo enterprise;
    
    @Schema(description = "院校档案完整度")
    private SchoolCompletenessVo school;
}
```

### 2. 设计师完整度详细信息

**文件**: `ruoyi-modules-api/ruoyi-designer-api/src/main/java/org/ruoyi/designer/domain/vo/DesignerCompletenessVo.java`

```java
package org.ruoyi.designer.domain.vo;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;

import java.util.List;

/**
 * 设计师档案完整度信息
 */
@Data
@Schema(description = "设计师档案完整度信息")
public class DesignerCompletenessVo {
    
    @Schema(description = "基础信息完整度 (0-100)", example = "85")
    private int basicInfo;
    
    @Schema(description = "作品集完整度 (0-100)", example = "60")
    private int portfolio;
    
    @Schema(description = "工作经历完整度 (0-100)", example = "75")
    private int experience;
    
    @Schema(description = "教育背景完整度 (0-100)", example = "100")
    private int education;
    
    @Schema(description = "总体完整度 (0-100)", example = "78")
    private int overall;
    
    @Schema(description = "改进建议列表")
    private List<String> suggestions;
    
    @Schema(description = "完整度等级", example = "GOOD", allowableValues = {"EXCELLENT", "GOOD", "FAIR", "POOR"})
    public String getLevel() {
        if (overall >= 90) return "EXCELLENT";
        if (overall >= 70) return "GOOD";
        if (overall >= 50) return "FAIR";
        return "POOR";
    }
}
```

### 3. 企业和院校完整度类

```java
// EnterpriseCompletenessVo.java
@Data
@Schema(description = "企业档案完整度信息")
public class EnterpriseCompletenessVo {
    private int basicInfo;
    private int contactInfo;
    private int businessInfo;
    private int overall;
    private List<String> suggestions;
}

// SchoolCompletenessVo.java
@Data
@Schema(description = "院校档案完整度信息")
public class SchoolCompletenessVo {
    private int basicInfo;
    private int achievementInfo;
    private int courseInfo;
    private int overall;
    private List<String> suggestions;
}
```

## 🎯 前端调用示例

### 1. 设计师档案管理流程

#### 1.1 统一档案管理接口调用

```typescript
// 工作经历管理 - 统一接口
class WorkExperienceService {
  
  // 查询工作经历列表（权限控制返回范围）
  static async getWorkExperienceList(): Promise<WorkExperience[]> {
    const response = await request.get('/designer/work-experience/list');
    return response.data;
  }
  
  // 新增工作经历
  static async addWorkExperience(workExperience: WorkExperience) {
    const response = await request.post('/designer/work-experience', workExperience);
    return response;
  }
  
  // 修改工作经历
  static async updateWorkExperience(experienceId: number, workExperience: WorkExperience) {
    const response = await request.put(`/designer/work-experience/${experienceId}`, workExperience);
    return response;
  }
  
  // 删除工作经历
  static async deleteWorkExperience(experienceId: number) {
    const response = await request.delete(`/designer/work-experience/${experienceId}`);
    return response;
  }
  
  // 查询指定设计师的工作经历（公开访问）
  static async getDesignerWorkExperience(designerId: number): Promise<WorkExperience[]> {
    const response = await request.get(`/designer/work-experience/designer/${designerId}`);
    return response.data;
  }
}

// 教育背景管理 - 统一接口
class EducationService {
  
  static async getEducationList(): Promise<Education[]> {
    const response = await request.get('/designer/education/list');
    return response.data;
  }
  
  static async addEducation(education: Education) {
    const response = await request.post('/designer/education', education);
    return response;
  }
  
  static async updateEducation(educationId: number, education: Education) {
    const response = await request.put(`/designer/education/${educationId}`, education);
    return response;
  }
  
  static async deleteEducation(educationId: number) {
    const response = await request.delete(`/designer/education/${educationId}`);
    return response;
  }
  
  static async getDesignerEducation(designerId: number): Promise<Education[]> {
    const response = await request.get(`/designer/education/designer/${designerId}`);
    return response.data;
  }
}

// 作品管理 - 统一接口
class WorkService {
  
  static async getWorkList(): Promise<Work[]> {
    const response = await request.get('/designer/work/list');
    return response.data;
  }
  
  static async addWork(work: Work) {
    const response = await request.post('/designer/work', work);
    return response;
  }
  
  static async updateWork(workId: number, work: Work) {
    const response = await request.put(`/designer/work/${workId}`, work);
    return response;
  }
  
  static async deleteWork(workId: number) {
    const response = await request.delete(`/designer/work/${workId}`);
    return response;
  }
  
  static async getDesignerWork(designerId: number): Promise<Work[]> {
    const response = await request.get(`/designer/work/designer/${designerId}`);
    return response.data;
  }
}

// 获奖记录管理 - 统一接口
class AwardService {
  
  static async getAwardList(): Promise<Award[]> {
    const response = await request.get('/designer/award/list');
    return response.data;
  }
  
  static async addAward(award: Award) {
    const response = await request.post('/designer/award', award);
    return response;
  }
  
  static async updateAward(awardId: number, award: Award) {
    const response = await request.put(`/designer/award/${awardId}`, award);
    return response;
  }
  
  static async deleteAward(awardId: number) {
    const response = await request.delete(`/designer/award/${awardId}`);
    return response;
  }
  
  static async getDesignerAward(designerId: number): Promise<Award[]> {
    const response = await request.get(`/designer/award/designer/${designerId}`);
    return response.data;
  }
}
```

#### 1.2 实际使用示例

```typescript
// 用户前端页面 - 管理自己的工作经历
const ManageWorkExperience: React.FC = () => {
  const [experiences, setExperiences] = useState<WorkExperience[]>([]);
  
  // 加载工作经历列表（权限控制自动返回当前用户的数据）
  const loadExperiences = async () => {
    try {
      const data = await WorkExperienceService.getWorkExperienceList();
      setExperiences(data);
    } catch (error) {
      message.error('加载工作经历失败');
    }
  };
  
  // 新增工作经历
  const handleAdd = async (formData: WorkExperience) => {
    try {
      await WorkExperienceService.addWorkExperience(formData);
      message.success('添加成功');
      loadExperiences(); // 刷新列表
    } catch (error) {
      message.error('添加失败');
    }
  };
  
  // 修改工作经历
  const handleUpdate = async (experienceId: number, formData: WorkExperience) => {
    try {
      await WorkExperienceService.updateWorkExperience(experienceId, formData);
      message.success('修改成功');
      loadExperiences(); // 刷新列表
    } catch (error) {
      message.error('修改失败');
    }
  };
  
  // 删除工作经历
  const handleDelete = async (experienceId: number) => {
    try {
      await WorkExperienceService.deleteWorkExperience(experienceId);
      message.success('删除成功');
      loadExperiences(); // 刷新列表
    } catch (error) {
      message.error('删除失败');
    }
  };
  
  useEffect(() => {
    loadExperiences();
  }, []);
  
  return (
    <div>
      {/* 工作经历列表和操作界面 */}
    </div>
  );
};

// 档案展示页面 - 查看他人档案
const DesignerProfilePage: React.FC<{ designerId: number }> = ({ designerId }) => {
  const [profile, setProfile] = useState<any>(null);
  
  // 加载设计师完整档案
  const loadDesignerProfile = async () => {
    try {
      const [workExperience, education, works, awards] = await Promise.all([
        WorkExperienceService.getDesignerWorkExperience(designerId),
        EducationService.getDesignerEducation(designerId),
        WorkService.getDesignerWork(designerId),
        AwardService.getDesignerAward(designerId)
      ]);
      
      setProfile({
        workExperience,
        education,
        works,
        awards
      });
    } catch (error) {
      message.error('加载档案失败');
    }
  };
  
  useEffect(() => {
    loadDesignerProfile();
  }, [designerId]);
  
  return (
    <div>
      {/* 展示设计师完整档案信息 */}
    </div>
  );
};
```

### 2. 档案完整度查询和展示

```typescript
// 获取档案完整度
const getProfileCompleteness = async () => {
  const response = await request.get('/designer/profile/completeness');
  return response.data;
};

// 使用示例
const completeness = await getProfileCompleteness();
if (completeness.designer) {
  console.log('设计师档案完整度：', completeness.designer.overall + '%');
  console.log('改进建议：', completeness.designer.suggestions);
}
```

### 3. 通用身份信息提取工具函数

```typescript
interface UserIdentities {
  designerId: number | null;
  enterpriseId: number | null;
  schoolId: number | null;
}

/**
 * 从用户绑定信息中提取各种身份ID
 */
const extractUserIdentities = (bindings: UserBinding[]): UserIdentities => {
  const identities: UserIdentities = {
    designerId: null,
    enterpriseId: null,
    schoolId: null
  };
  
  bindings.forEach(binding => {
    if (binding.bindingStatus === '1') { // 1表示有效绑定
      switch (binding.entityType) {
        case 'designer':
          identities.designerId = binding.entityId;
          break;
        case 'enterprise':
          identities.enterpriseId = binding.entityId;
          break;
        case 'school':
          identities.schoolId = binding.entityId;
          break;
      }
    }
  });
  
  return identities;
};

// 使用示例
const bindings = await getUserBindings();
const { designerId, enterpriseId, schoolId } = extractUserIdentities(bindings);

// 根据不同身份执行不同操作
if (designerId) {
  // 设计师相关操作
  const workExperiences = await getWorkExperience();
}

if (enterpriseId) {
  // 企业相关操作
  const jobs = await getEnterpriseJobs(enterpriseId);
}

if (schoolId) {
  // 院校相关操作
  const students = await getSchoolStudents(schoolId);
}
```

## 🚀 实施优先级

### 第一阶段：用户自助接口实现（高优先级）

1. **用户自助接口开发**
   - [ ] 在 `UserRegistrationController` 中添加用户自助管理接口
   - [ ] 实现工作经历自助管理：`/designer/work-experience/*`
   - [ ] 实现教育背景自助管理：`/designer/education/*`
   - [ ] 实现作品自助管理：`/designer/works/*`
   - [ ] 实现获奖记录自助管理：`/designer/awards/*`

2. **权限和安全实现**
   - [ ] 实现 `getCurrentDesignerId()` 等工具方法
   - [ ] 添加身份验证和权限检查逻辑
   - [ ] 实现数据安全验证（防止恶意修改）

### 第二阶段：档案查询接口完善（中优先级）

3. **档案查询接口开发**
   - [ ] 添加按设计师ID查询的档案接口：`/designer/*/designer/{designerId}`
   - [ ] 实现基本参数验证
   - [ ] 开发聚合API：`/designer/designer/{designerId}/complete`

4. **完整度计算功能**
   - [ ] 添加 `/designer/profile/completeness` 接口
   - [ ] 创建 ProfileCompletenessVo 等 VO 类
   - [ ] 实现完整度计算逻辑和建议生成

### 第三阶段：系统功能扩展（低优先级）

5. **系统功能扩展**
   - [ ] 智能推荐功能
   - [ ] 导入导出功能
   - [ ] 数据分析和报表功能

## 📝 实施注意事项

### 1. 权限控制原则

- **超级管理员**：拥有所有档案信息的查看和编辑权限
- **设计师用户**：只能查看和编辑自己的档案信息
- **公开访问**：所有用户都可以查看设计师的档案信息（无需特殊权限）

### 2. 安全机制

- **身份验证**：所有接口都需要用户登录
- **权限校验**：通过 `DesignerPermissionUtils.hasDesignerPermission()` 验证
- **数据隔离**：防止用户修改他人的档案信息
- **参数安全**：防止恶意修改 designerId 等关键参数

### 3. 错误处理

- **参数验证**：严格验证输入参数的合法性
- **数据不存在**：优雅处理数据不存在的情况
- **系统异常**：返回明确的错误信息，不暴露系统内部信息

### 4. 性能考虑

- **分页查询**：list 接口支持分页，避免一次性加载过多数据
- **缓存策略**：可考虑对完整度计算结果进行缓存
- **数据库优化**：确保相关查询有适当的索引

## 📋 API 接口汇总

### 1. 统一档案管理接口

```bash
# 工作经历管理
GET    /designer/work-experience/list              # 查询工作经历列表（权限控制返回范围）
POST   /designer/work-experience                   # 新增工作经历
PUT    /designer/work-experience/{experienceId}    # 修改工作经历
DELETE /designer/work-experience/{experienceId}    # 删除工作经历

# 教育背景管理
GET    /designer/education/list                    # 查询教育背景列表（权限控制返回范围）
POST   /designer/education                         # 新增教育背景
PUT    /designer/education/{educationId}           # 修改教育背景
DELETE /designer/education/{educationId}          # 删除教育背景

# 作品管理
GET    /designer/work/list                         # 查询作品列表（权限控制返回范围）
POST   /designer/work                              # 新增作品
PUT    /designer/work/{workId}                     # 修改作品
DELETE /designer/work/{workId}                    # 删除作品

# 获奖记录管理
GET    /designer/award/list                        # 查询获奖记录列表（权限控制返回范围）
POST   /designer/award                             # 新增获奖记录
PUT    /designer/award/{awardId}                   # 修改获奖记录
DELETE /designer/award/{awardId}                  # 删除获奖记录
```

### 2. 档案查询接口（公开访问）

```bash
# 设计师档案查询
GET    /designer/work-experience/designer/{designerId}  # 查询指定设计师的工作经历
GET    /designer/education/designer/{designerId}        # 查询指定设计师的教育背景
GET    /designer/work/designer/{designerId}             # 查询指定设计师的作品
GET    /designer/award/designer/{designerId}            # 查询指定设计师的获奖记录
GET    /designer/designer/{designerId}/complete         # 查询设计师完整档案（聚合API）
```

### 3. 通用功能接口

```bash
# 用户绑定信息查询（已存在）
GET    /designer/bindings                          # 获取用户绑定信息

# 档案完整度功能
GET    /designer/profile/completeness              # 获取档案完整度

# 设计师基础信息（已存在）
GET    /designer/designer/list                     # 查询设计师列表
GET    /designer/designer/{designerId}             # 获取设计师基础信息
```

## 🧪 测试验证

### 1. 权限测试

```bash
# 测试设计师查询自己的工作经历
GET /designer/work-experience/list
# 期望：返回当前用户的工作经历列表

# 测试设计师修改自己的工作经历
PUT /designer/work-experience/123
# 期望：成功修改属于自己的工作经历

# 测试设计师修改他人的工作经历
PUT /designer/work-experience/456
# 期望：返回权限错误
```

### 2. 功能测试

```bash
# 测试完整档案更新流程
1. GET /designer/bindings                          # 获取绑定信息
2. 从绑定信息中提取designerId               # 前端处理
3. GET /designer/work-experience/list              # 查询工作经历
4. PUT /designer/work-experience/123          # 更新工作经历
5. GET /designer/profile/completeness         # 查看完整度变化
```

## 📈 预期效果

### 1. 用户体验提升

- **自主管理**：设计师可以完全自主管理自己的档案信息
- **便捷操作**：使用现有绑定接口，无需了解复杂的权限体系
- **智能引导**：通过完整度提示，指导用户完善档案

### 2. 系统安全性

- **权限隔离**：确保用户只能操作自己的数据
- **数据安全**：防止恶意修改和数据泄露
- **审计跟踪**：保留完整的操作日志

### 3. 开发效率

- **接口复用**：基于现有接口进行优化，减少开发工作量
- **权限统一**：使用统一的权限检查逻辑
- **维护简单**：清晰的接口设计，便于后续维护

## 🎯 方案优势

### 1. 统一的接口设计
- ✅ **路径规范统一**：使用单数形式资源名，参数名称一致
- ✅ **权限控制智能**：通过后端权限检查自动控制返回数据范围
- ✅ **接口复用高效**：一套接口支持多种用户场景

### 2. 优秀的用户体验
- 🎯 **简化前端调用**：减少接口数量，降低前端复杂度
- 🔐 **安全可靠**：自动身份验证和权限控制
- ⚡ **响应迅速**：统一的权限检查逻辑，性能优化

### 3. 简化的访问控制
- 🛡️ **智能权限分发**：根据用户角色自动返回相应数据
- 🌐 **公开档案访问**：专门的查询接口支持公开访问
- 📊 **统一操作审计**：所有操作使用相同的日志记录

### 4. 架构扩展性
- 🏗️ **统一设计模式**：所有档案管理接口使用相同的设计模式
- 🔧 **通用权限工具**：可扩展到其他模块的权限控制
- 📈 **性能优化**：统一的权限检查和数据过滤逻辑

---

**文档版本**：v5.0 (统一版)  
**创建时间**：2025-01-27  
**维护者**：开发团队  

**更新日志**：
- v5.0 (2025-01-27): 统一接口设计，消除重复接口，采用智能权限控制
- v4.1 (2025-01-27): 去掉接口路径中的"user"，简化接口路径设计
- v4.0 (2025-01-27): 去掉管理员接口，简化方案，只保留用户自助接口和档案查询接口
- v3.1 (2025-01-27): 基于"用户信息基本公开"的项目特点，简化档案查询接口设计
- v3.0 (2025-01-27): 采用方案1设计，明确场景分工，实现三类接口设计
- v2.0 (2025-01-27): 优化方案，移除不必要的身份ID获取接口
- v1.0 (2025-01-27): 初始版本，包含完整的接口优化方案和实施计划 